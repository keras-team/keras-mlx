# MLX. The base wheel only ships the Python frontend. The native library
# comes from mlx-metal on macOS and mlx-cpu on Linux (the `cpu` extra,
# which is platform-gated to Linux so macOS still gets Metal).
# Pinned below 0.32 because mlx 0.32.0 made stream state thread-local and
# evaluating an array whose graph was recorded on another thread now fails.
# That breaks tf.data generator threads, multithreaded `PyDataset` and grain
# loaders. See https://github.com/ml-explore/mlx/issues/3529.
mlx[cpu]<0.32

# Tensorflow
tensorflow-cpu;sys_platform != 'darwin'
tensorflow==2.19.1;sys_platform == 'darwin'

# Torch.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Jax.
jax[cpu]==0.10.2

grain==0.2.11
array_record==0.7.2

# Testing
pytest
pytest-xdist
